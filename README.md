use `Postman` for testing URL to get authentication or you open this local host for testing: http://127.0.0.1:8001/docs#/

run this command step by step
source ./venv/bin/activate
.venv/bin/uvicorn app.main:app --reload
